name: Scheduled Tasks
on:
    schedule:
        - cron: '0 0 * * *'
jobs:
    tasks:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v3
        - name: Run Tasks
          run: npm run tasks